export type User = {
    nome: string
    email: string
    birthDate: string
    token?: string
    id: number
}

export type UserPT = {
    nome: string
    email: string
    dt_nascimento: string
    id_usuario: string
}